{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome on local files",
            "url": "${workspaceFolder}/home/index.html",
            "webRoot": "${workspaceFolder}"
        }
    ]
}