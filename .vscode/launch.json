{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome on local files",
            "url": "http://127.0.0.1:5500/home/",
            "webRoot": "${workspaceFolder}"
        }
    ]
}